import React, { Component } from 'react';
import { Layout, Button } from 'antd';
const { Content } = Layout;

export default class TravelsServices extends Component {
	render() {
		return (
			<Content className="mainContent">
				Services For Travels
			</Content>
		);
  }
}
