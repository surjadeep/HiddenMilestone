import React, { Component } from 'react';
import { Layout, Button } from 'antd';
const { Content } = Layout;

export default class TravelsHome extends Component {
	render() {
		return (
			<Content className="mainContent">
				Travels > Home
			</Content>
		);
  }
}
