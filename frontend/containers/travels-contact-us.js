import React, { Component } from 'react';
import { Layout, Button } from 'antd';
const { Content } = Layout;

export default class TravelsContactUs extends Component {
	render() {
		return (
			<Content className="mainContent">
				Contact Us For Travels
			</Content>
		);
  }
}
